import React, { useState } from "react";
import Balance from "./balance";

function Dashboard() {
    return (
        <div>
            <p>Dashboard</p>
            <Balance />
        </div>
    );
}

export default Dashboard;